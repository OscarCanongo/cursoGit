-- Deshacer commits
git rm --cached

-- Regresa a una version anterior
con git log puedo ver las versiones
git reset [id] --hard || -soft

--Volver a un archivo anterior 
git checkout [id] || branch fileName
